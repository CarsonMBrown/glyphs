def link_bboxes(bboxes):
    """
    Takes in a list of pascal formatted (x_min, y_min, x_max, y_max) bounding boxes and generates a list of lists of bounding boxes
    representing lines
    :param bboxes: list of bounding boxes in pascal form (x_min, y_min, x_max, y_max)
    :return: list of lists of bounding boxes
    """
    lines = []
    for bbox in bboxes:
        pass
    return lines
